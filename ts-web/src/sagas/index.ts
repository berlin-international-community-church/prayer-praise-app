import {
  userProfile
} from './userProfile';

import {
  tokenData
} from './tokenData';

import {
  messages
} from './messages';

import {
  sharedMessages
} from './sharedMessages';

import {
  myMessages
} from './myMessages';

import {
  deleteMessage
} from './deleteMessage';

export default [ userProfile, tokenData, messages, sharedMessages, myMessages, deleteMessage ];
