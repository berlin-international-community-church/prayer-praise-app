import {
  userProfile
} from './userProfile';

import {
  tokenData
} from './tokenData';

import {
  messages
} from './messages';

import {
  sharedMessages
} from './sharedMessages';

import {
  myMessages
} from './myMessages';

export default [ userProfile, tokenData, messages, sharedMessages, myMessages ];
