import {
  userProfile
} from './userProfile';

import {
  tokenData
} from './tokenData';

import {
  messages
} from './messages';

import {
  sharedMessages
} from './sharedMessages';

export default [ userProfile, tokenData, messages, sharedMessages ];
