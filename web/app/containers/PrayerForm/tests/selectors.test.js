// import { fromJS } from 'immutable';
// import { makeSelectPrayerFormDomain } from '../selectors';

// const selector = makeSelectPrayerFormDomain();

describe('makeSelectPrayerFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
